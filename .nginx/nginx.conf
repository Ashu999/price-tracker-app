http {
    server {
        listen 3000;
        location / {
            proxy_pass "http://localhost:3000";
        }
        
        location /api {
            rewrite ^/api(.*) $1 break;
            proxy_pass "http://localhost:8080";
        }
    }
}

# add include line in "/etc/nginx/nginx.conf": "include /home/price-tracker-app/.nginx/*.conf;"